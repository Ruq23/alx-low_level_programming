#include "main.h"
#include <string.h>

/**
* _strlen - function that returns the length of a string
* @s: first int argument
* Return: length of a string
*/

int _strlen(char *s)
{
	return (strlen(s));
}
