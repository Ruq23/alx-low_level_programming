#include "main.h"
#include <string.h>

/**
* _strcmp - function strcmp
* @s1: first argument
* @s2: second argument
* Return: an int
*/

int _strcmp(char *s1, char *s2)
{
	inst result;
	result = strcmp(s1, s2);
	return (result);
}
